import { executeQuery, types } from '../../../utils/dbUtils';

export default function AuthDbService() {
    const _getPasswordHashByUsername = (userName) => {

    };

    const _validatePasswordHash = (userName, password) => {
        
    };

    this.updateUserPassword = async () => {

    };
};

const _queries = {

};
